// => we use this symbol
// fat array functions 
//aka array functions

const add = (a,b) => a+b;
    console.log(add(1,2)); // being a single line, we can leave out the 'return'

 const subtract =(a,b) =>{ //unlike the above code, here 
    //we used return since it was more than one line
    return a-b
 };   
 console.log(subtract(2,1));   