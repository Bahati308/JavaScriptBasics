// this is about loops or iteration

//while, for ,and for ......of are the most common

const names = ['jane', 'jackie', 'christine'];

console.log('----while loop-----');

let whileIndex = 0;

while (whileIndex < names.length) {
    const name = names [whileIndex];
console.log(name);
    whileIndex ++;
}