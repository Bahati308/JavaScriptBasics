// we are manipulating arrays in this progam

// pop and push

let arr1 = ['A', true, 9];

console.log (arr1.push('new value'));//adds one more value
// and returns its length

console.log(arr1);// now this will have new values from
// those of the original

console.log(arr1.pop()); //removes value from end of the 
// array and returns removed vlue
console.log(arr1);

