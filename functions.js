// a function is just a block of executible code
// they are called by simply using the 'function' key word

function printHello(name) {
    console.log('hello ', name);
}

console.log(typeof printHello);// this defines its type

//this step is so crucial
printHello('Bahati308'); // this invokes the function