// the for loop

const names = ['baptist', 'eric', 'douglas'];

console.log('---for loop----');
for (let forindex = 0; forindex < names.length; forindex++) {
    const element = names[forindex];
    console.log(element);
}